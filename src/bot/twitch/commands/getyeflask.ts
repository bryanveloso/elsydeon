import { createBotCommand } from '@twurple/easy-bot'

export const getyeflask = createBotCommand('getyeflask', (_params, { say }) => {
  say(`This command is currently under construction. You get a flask. There's nothing in it. It's a supply chain issue.`)
})
